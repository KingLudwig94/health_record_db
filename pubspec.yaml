name: health_record_db
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  floor:
  path: ^1.8.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  #Dev-dependencies needed for Floor 
  floor_generator: ^1.2.0
  build_runner: ^2.1.2

flutter:
  uses-material-design: true
